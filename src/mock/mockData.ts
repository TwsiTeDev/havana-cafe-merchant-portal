const MOCK_MENU = [...];
const MOCK_CUSTOMERS = [...];
const MOCK_ORDERS = [...];
const MOCK_BAYS = [...];
const MOCK_ANALYTICS_DAILY = [...];
const MOCK_TOP_ITEMS = [...];
const STATUS_COLORS = [...];

export {
  MOCK_MENU,
  MOCK_CUSTOMERS,
  MOCK_ORDERS,
  MOCK_BAYS,
  MOCK_ANALYTICS_DAILY,
  MOCK_TOP_ITEMS,
  STATUS_COLORS,
};